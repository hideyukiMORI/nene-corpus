# 2026-07-18

hub 指揮の小粒 docs 正直化を Issue 駆動で 3 本 main へ届けた（CLAUDE.md ×2・`docs/todo/current.md`）。
過程で E2E スイートを全量実測し、admin スイートの spec×現行 UI 乖離（147 fail）を切り分けまで含めて定量化。
再整備と CI 配線は W4 向け Issue として起票のみ（着手せず）。夜に日報のフリート規約移行（本ファイル含む）を実施。

---

## CLAUDE.md 正直化 2 本（マージ済み）

- **#347 / PR #349**（main `b5fe714`・マージ済み）: 品質チェック節の frontend check コメントを「TypeScript + lint」→「lint (ESLint) + typecheck + test (vitest)」へ実態合わせ。
  - 発端は hub 査定の発見13「lint 不在」だったが、`frontend/eslint.config.js`（#341/#342 で導入済み）と `npm run check` が lint 込みで exit 0〔実測〕を確認し、前提が時点測定で古いと差し戻し → hub 承認の再定義で逆方向の修正になった。
- **#348 / PR #350**（main `f01ea20`・マージ済み）: CLAUDE.md に「E2E テスト（`tests/e2e/` — CI 未配線・手動実行のみ）」節を新設。実行コマンド・実測結果・CI 未配線の事実を記録。
- 残タスク: なし（両 PR とも CI 緑・squash self-merge・job 名/required 不触）。

---

## E2E 全量実測（admin 147 fail の切り分け）

- widget スイート（`tests/e2e/specs/` 103 テスト）: **103/103 pass・約1.1分**〔実測〕
- admin スイート（`tests/e2e/admin/specs/` 373 テスト）: **226 pass・147 fail・約58分**〔実測〕
- 切り分け: `npm run build:release:admin` で最新ソースから再ビルドしても `01-auth` は **3/17**〔実測〕→ 落ちる原因はコミット済みビルドの古さではなく **spec と現行 admin UI の乖離**（例: `input[type="email"]` の prefill 期待が現 UI に無い）。確認後 `public_html/` は `git restore` 済み。
- 教訓★: CI に配線されていないテストの drift は誰にも見えない。「157/157 グリーン」という記録（2026-05-28 当時）と今日の 226/373 の差が、未配線期間に静かに積もった。

## current.md 正直化（hub 裁定・最優先）

- **#353 / PR #354**（main `8c9da5a`・マージ済み）: `docs/todo/current.md` の冒頭に「E2E テストの実態（2026-07-18 実測）」節を新設し、上記実測値を正として宣言。「157/157 グリーン」2箇所は**消さずに当時注記＋実測参照**で訂正（歴史は書き換えない）。#316〜#350 のマージ履歴を3グループで「最近の docs 更新」に反映。

## W4 向け Issue 起票（着手せず）

- **#351**: admin spec の現行 UI 再整備（147 fail 解消）
- **#352**: e2e の CI 配線（#351 green 化が先行条件・順序固定は hub 裁定）
- ラベル `W4` を新設して両方に付与。着手は W4 号令待ち。

## 日報のフリート規約移行（本 PR・#355）

- `docs/daily-reports/` の日付ファイル4件を `git mv` で `docs/daily/` へ（履歴保持）。E2E 引き継ぎ書は `docs/testing/` へ分離。索引 `docs/daily/README.md` 新設・CLAUDE.md に1行参照追加。根拠はフリート共通規約（`_work/daily-report-convention.md`・付録 A の corpus 行）。

## その他

- articles リナへ記事ネタ1本を relay 供給（CI 未配線 E2E の静かな腐り＋査定の鮮度、一次資料 #347〜#350）。送達 ok〔実測〕。
- ayane.co.jp が NeNe Records SSR スタックで刷新ローンチ〔伝聞・hub 同報より。本リポの作業ではない〕。

## 📊 本日の数字

- マージ 3 PR: #349（`b5fe714`）・#350（`f01ea20`）・#354（`8c9da5a`）— いずれも docs のみ・CI 緑〔実測〕
- 起票 5 Issue: #347・#348・#351・#352・#353（+ 本 #355）
- E2E: widget 103/103・admin 226/373・再ビルド後 01-auth 3/17〔いずれも実測〕
